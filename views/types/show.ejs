<%- include('../partials/header') %>

<br>
<h1><%= title %></h1>
<br>
<form id="change-type-name" action="/types/<%= type._id %>?_method=PUT" method="POST">
    
    <label class="raleway-bold">Change Name?</label><br>
    <input class="type-edit" type="text" name="name" value="<%= type.name %>">

    <input type="submit" value="Save">
</form>

<br><br>
<section>
    <div class="label-d button-label type-wines">
        <p class="label-p raleway-bold">Wines</p>
    </div>
    <div class="multi type-wines">
        <% if (wines.length) { %> 
            <% wines.forEach(function(w) { %>
                <div>
                    <a href="/wines/<%= w._id %>"><p class="value-p"><%= w.name %></p></a>
                </div>
            <% }) %>
        <% } %>
    </div>
</section>



<br>
<br>
<a href="/types/<%= type._id %>/delete"><button class="large-button">Delete This Type</button></a>

<%- include('../partials/footer') %>